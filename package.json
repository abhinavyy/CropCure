{
  "name": "cropcure",
  "version": "1.0.0",
  "description": "CropCure - AI-Powered Agricultural Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run backend\" \"wait-on http://127.0.0.1:10000 && npm run frontend\"",
    "backend": "cd backend && (python check_dependencies.py && python app.py || python3 check_dependencies.py && python3 app.py)",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "npm run dev",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "postinstall": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.4"
  },
  "keywords": [
    "agriculture",
    "crop",
    "disease-detection",
    "ai",
    "flask",
    "react"
  ],
  "author": "",
  "license": "MIT"
}
